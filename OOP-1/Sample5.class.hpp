#ifndef SAMPLE5_CLASS_H
# define SAMPLE5_CLASS_H

//structures anc lasses works exactly in the same way in c++
//why not use struct only or class only
//cause actually there is some differents:

struct Sample5{

    int foo;

    Sample5( void );
    ~Sample5( void );

    void bar( void ) const;
};



#endif